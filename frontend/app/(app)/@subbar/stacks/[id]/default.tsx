/**
 * Default fallback for stack detail SubBar parallel route.
 * Returns null when route doesn't match or during navigation.
 */
export default function StackDetailSubBarDefault() {
  return null
}
