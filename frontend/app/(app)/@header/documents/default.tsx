import { PageHeader } from '@/components/layout/page-header'

/**
 * Default fallback for documents routes without specific header.
 * Shows simple Documents breadcrumb.
 */
export default function DocumentsHeaderDefault() {
  return <PageHeader />
}
